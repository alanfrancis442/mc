type Dimensions = {
    width: number;
    height: number;
    pixelRatio: number;
};

type Sizes = {
    width: number;
    height: number;
};
type BlockData = {
    id: number,
    instanceId: number | null,
}[][][]
